{
  "name": "my-fitness-journey",
  "version": "1.0.0",
  "description": "A personal fitness journey tracking application",
  "main": "src/index.js",
  "scripts": {
    "build": "cp -r src dist && cp -r public/* dist/",
    "start": "npx serve dist",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "keywords": [
    "fitness",
    "health",
    "workout",
    "tracker"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "serve": "^14.2.0"
  }
}
